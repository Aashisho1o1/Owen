# ============================================================================
# DOG WRITER - GOOGLE AI COMPETITION - MINIMAL REQUIREMENTS
# ============================================================================
# Only includes essentials for Gemini API + FastAPI backend

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.18
python-dotenv==1.0.0

# Authentication & Security
PyJWT==2.8.0
bcrypt==4.2.1
email-validator==2.2.0
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
cryptography>=42.0.2

# HTTP requests & async support
requests==2.32.4
aiohttp==3.12.14
aiofiles==23.2.1

# Google Gemini AI - THE ONLY AI SERVICE WE NEED!
google-generativeai==0.8.3

# Database (PostgreSQL only)
psycopg2-binary==2.9.9

# Redis for rate limiting
redis==5.0.1

# Templating
jinja2==3.1.6

# Performance monitoring
psutil==6.1.0

# ============================================================================
# REMOVED FOR GOOGLE AI COMPETITION:
# - OpenAI (not needed)
# - HuggingFace transformers (not needed)
# - PyTorch (HUGE, not needed)
# - ChromaDB (not needed)
# - Sentence transformers (not needed)
# - Spacy (not needed)
#
# Result: ~95% smaller deployment, much faster!
# ============================================================================

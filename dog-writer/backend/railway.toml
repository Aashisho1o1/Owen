[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/api/health"
healthcheckTimeout = 60

[variables]
RAILWAY_SERVICE_NAME = "owen-backend-fixed" 
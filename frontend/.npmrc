# Ensure all optional dependencies are installed
optional=true

# Install all platform-specific binaries
target_platform=linux
target_arch=x64

# Disable platform checks for Rollup binaries
force=true

# Use npm ci for consistent installations
package-lock=true

# Increase timeout for slow networks
timeout=600000 


####


